import {PackagesContent} from "@/modules/landing/domain/landingModel";
import Image from "next/image";

export function SinglePackages({ silver, platinum, gold }: PackagesContent) {
    return (
        <section className="container max-w-screen-xl mx-auto px-4">


        </section>
    );
}
