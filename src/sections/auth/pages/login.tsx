import LoginCard from "@/sections/auth/components/organisms/login-card";

/**
 * LoginCard component that contains the logo and login form
 */
const LoginView: React.FC = () => {
    return (

        <LoginCard/>
    );
};

export default LoginView;

